begin
 int pattern[6];
 int i;
 i := 0;
 while i < 6 do 
    begin
        if (i / 2) * 2 = i then 
            pattern[i] := 0 
        else 
            pattern[i] := 1; 
            i := i + 1
    end
end